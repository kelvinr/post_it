-if comment.persisted?
  .container
    .col-md-8
      .well
        %p= comment.body
        %span.quiet
          =surround "#{comment.creator.name} | ", " ago" do
            =display_time(comment.created_at)


    

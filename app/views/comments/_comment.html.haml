%h4 Comments
.col-md-8.col-md-offset-2
  .well
    %p= comment.body 
    %p.quiet
      =surround "#{comment.creator.name} | ", " ago" do
        =time_ago_in_words(comment.created_at) unless comment.body.empty?